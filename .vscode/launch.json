{
    "version": "0.2.0",
    "configurations": [{
        "name": "Python",
        "type": "python",
        "request": "launch",
        "program": "${workspaceRoot}/src/mailme/main.py",
        "env": {
            "PYTHONPATH": "${workspaceRoot}/src:{env.PYTHONPATH}"
        }
    }]
}
